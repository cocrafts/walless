import { Text } from '@walless/ui';

const Header = () => {
	return (
		<Text fontSize={30} maxWidth={600} textAlign="center">
			Having <Text color="#0694D3">your own custom layout</Text> in Walless has
			never been easier. Try it now!
		</Text>
	);
};

export default Header;
