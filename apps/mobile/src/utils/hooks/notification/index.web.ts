import { useEffect } from 'react';

export const useNotifications = () => {
	useEffect(() => {
		// unlike Mobile, for Extension build notification  already configured at IOC
	}, []);
};

export const useNotificationPermissionRequest = () => {
	useEffect(() => {
		// unlike Mobile, for Extension build notification  already configured at IOC
	}, []);
};
