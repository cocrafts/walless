import { StyleSheet } from 'react-native';
import { Text, View } from '@walless/gui';

export const Drawer = () => {
	return (
		<View>
			<Text>index</Text>
		</View>
	);
};

const styles = StyleSheet.create({});
