import { StyleSheet } from 'react-native';
import { Text, View } from '@walless/gui';

const TransactionResult = () => {
	return (
		<View>
			<Text>TransactionResult</Text>
		</View>
	);
};

export default TransactionResult;

const styles = StyleSheet.create({});
