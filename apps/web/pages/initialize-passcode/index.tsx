import React from 'react';
import { ExtensionContainer, Text } from '@walless/wui';

const InitializePasscode: React.FC = () => {
	return (
		<ExtensionContainer justifyContent="center">
			<Text>InitializePasscode</Text>
		</ExtensionContainer>
	);
};

export default InitializePasscode;
