import { showRequirePasscodeModal } from './passcode';
import { showError } from './system';
import { showSendTokenModal } from './token';

export const floatActions = {
	showError,
	showSendTokenModal,
	showRequirePasscodeModal,
};
