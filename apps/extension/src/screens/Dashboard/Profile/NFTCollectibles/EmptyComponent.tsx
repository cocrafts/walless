import { Text, View } from '@walless/ui';

const EmptyComponent = () => {
	return (
		<View>
			<Text className="text-xs">There are no collectibles</Text>
		</View>
	);
};

export default EmptyComponent;
