import { Stack } from '@walless/ui';

const SocialCard = () => {
	return (
		<Stack backgroundColor="#172028" padding={16} borderRadius={10}>
			<Stack height={120} minWidth={250} />
		</Stack>
	);
};

export default SocialCard;
